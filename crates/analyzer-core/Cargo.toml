[package]
name = "dbopt-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "The dbopt database performance analyzer engine: token-level query rules, execution-plan parsing and metrics-driven index advice. Engine-agnostic core, SQL Server rules today."
repository.workspace = true
readme = "README.md"
keywords = ["sql", "database", "performance", "linter", "sqlserver"]
categories = ["database", "development-tools"]

[lib]
name = "analyzer_core"
crate-type = ["rlib"]

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
quick-xml = { workspace = true }
regex = { workspace = true }
once_cell = { workspace = true }
thiserror = { workspace = true }
