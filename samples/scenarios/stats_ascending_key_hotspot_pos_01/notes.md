Recent-window scan against an ascending key (`OrderDate`). The histogram's right-most step usually lags real-world inserts, so the optimizer underestimates the trailing range and may pick a nested-loops plan over a more appropriate scan/seek. Locked as a future-hook scenario: no current rule should fire, and unrelated `stats.*` rules must stay silent so we can validate the eventual ascending-key detection without false positives.
