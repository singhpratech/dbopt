Classic Parameter-Sensitive Plan candidate: skewed `@status` distribution that benefits from multiple cached plans on SQL Server 2022+. The single `OPTION (RECOMPILE)` here defeats PSP without crossing the overuse threshold (which needs 3+ occurrences). Locked as a future-hook scenario: no current rule fires, and `plan.option_recompile_overuse` must stay silent because only one RECOMPILE hint is present.
