# Rust
/target/

# Node / web
node_modules/
web/dist/
web/src/wasm/

# Build & release artifacts — publish via GitHub Releases, never commit
*.tar.gz
*.zip
*.dmg
*.AppImage
*.msi
*.sha256
SHA256SUMS

# Local scratch / render previews
web/_*.mjs
web/_*.png
web/public/_*

# OS / editor
.DS_Store

# TS build cache
*.tsbuildinfo

# workflow-tool isolation worktrees (transient)
.claude/worktrees/

# Internal-only: strategy, roadmaps, research, scratch. Never published.
private/

# npm publish staging (generated by wasm-pack; rebuild with the release script)
npm/node/
npm/web/
npm/*.tgz
